(library
 ((name async_smtp)
  (public_name async_smtp)
  (libraries
   (async
    async_extended
    async_ssl
    core_extended.base64
    core
    core_extended
    textutils
    email_message
    re2
    json-wheel_jane_street_overlay
    logstash_conv
    ))
  (inline_tests ((deps (test-ca.crt test-server.crt test-server.key))))
  ))
(ocamllex (mail_from_lexer))

(enforce_style ((exceptions (mail_from_lexer.ml))))

(jbuild_version jane_street)
