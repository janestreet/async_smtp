(library
 ((name async_smtp_command)
  (public_name async_smtp.command)
  (libraries (async
              core
              email_message
              re2
              async_smtp))
  (preprocess (pps (ppx_jane ppxlib.runner)))
  ))


(jbuild_version 1)
